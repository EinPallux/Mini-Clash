# Changelog

All notable changes to Mini Clash are documented here.
Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/); versions follow the release plan in [ROADMAP.md](ROADMAP.md) (pre-1.0: `0.MINOR.PATCH`, one MINOR per roadmap phase).

Entry categories: `Added` · `Changed` · `Fixed` · `Balance` · `Content` · `Assets` · `Docs`

## [Unreleased] — v0.4.0 "Tag Team" (in progress)

### Added
- **Tag Swap is playable (GAME_DESIGN §7.2, UI_UX §8):** **Space** swaps your duo mid-match. The morph is **client-predicted** — the squash-and-pop starts on the keypress rather than a round trip later, measured at **1 ms input→morph over a 544 ms round trip** (the ROADMAP acceptance asks for ≤50 ms at 80 ms RTT). The presentation reads as one motion: a white-and-gold puff at the feet, the model squashing to nothing and popping back out with the rig exchanged exactly at the midpoint so the switch is never visible, a ring flash and light pulse, an arrival chime, and the incoming champion's entrance effect. The **duo panel** sits beside the ability cluster: the benched portrait ringed by the 9 s swap radial (label flips to SPACE when ready), dimmed while on cooldown, glinting when the bench brings both a castable ability and the Energy to pay for it, with its own Energy bar alongside — swapping into an empty bar is a real cost. Duo state rides the binary codec: the timers share the cooldown block (they tick together) and the bench identity rides the rare block (it only moves on a swap), keeping the wire at ~9.7 KB/s against the 12 KB/s budget. Space is rebindable in Settings → Controls; the lag smoke now measures swap feel in-page every CI run.
- **Duo dealing in champion select (GAME_DESIGN §7.1):** the Deal now hands every player a **pair**. Your duo lands as two chained cards (ACTIVE ♦ BENCH) with a dice chip on each — the 2 rerolls are spent per-slot, so you can re-draw the half you don't want and keep the one you do. Rerolled champions slide to the shared team bench and swap back into whichever slot you tap (the selected slot is outlined, and the bench label says where it will land). A reroll always changes the card, and a duo never holds the same champion twice — including when the roster is smaller than the deal wants (a 4-duo team needs 8 distinct champions; with 6 the deal exhausts the roster before it repeats, and Boltz and Wisp close that gap later this phase). The timer moves 45 s → 60 s per the design. Ally seats show their pairs live, enemy silhouettes carry a duo marker but stay hidden, and the tip line reads the passive of whichever half you have selected plus a Space-swap reminder. Both drivers deal duos: offline locally, and the lobby **server-side** (`LobbyRoom` deals 8 team-unique champions as 4 pairs, validates per-slot rerolls and bench swaps, and composes a roster where every seat carries `benchId`). The loading veil shows both halves of each ally pair. Covered by the updated lobby round-trip test and both CI browser smokes.
- **Tag Swap sim core (GAME_DESIGN §7.2):** duos live in the simulation — `MatchPlayerConfig` takes a `benchId`, and the champion entity carries a duo bank: the benched champion's kit, cooldowns (which keep ticking), Energy (which keeps refilling — swapping is a resource play), and passive scratch. The `swap` intent exchanges the banks in place: HP (one shared pool, the average of both champions' HP curves — symmetric, so swapping never moves your health bar), position, items, level, gold, buffs and shields all stay put. 9 s swap cooldown, 0.35 s morph (you keep moving and stay targetable, but attacks/casts wait), +20% move speed decaying over 1 s on swap-in, and the arriving champion's entrance effect fires. Blocked while channeling, leaping, stunned or airborne — a basic-attack windup just cancels. Death kills the duo on one timer; respawn restores both Energy bars with swap ready. Solo configs (v0.3 shape) are untouched. Seven edge-case sim tests: shared-pool math across swaps and level-ups, per-champion banks with bench recovery, morph lockout, channel/CC blocks, buff carryover, swap-adjacent projectile flight, death-frame handling, and snapshot duo blocks.

## [Unreleased] — v0.3.0 "Online" (in progress)

### Added
- **`@mini-clash/server` — the authoritative game server (TECH §2/§5):** a Colyseus room hosts the same deterministic sim the offline worker runs, stepped at 30 Hz with 20 Hz downstream snapshots. Everything a client sends is validated server-side: seat identity comes from the connection (wire player ids are ignored), intents are rate-limited (30/s), trainer/debug intents are rejected, and rosters are shape-checked with a safe default fallback. Per-team snapshot views keep brush concealment and ping scoping server-enforced — a modified client cannot see through brush or read enemy pings. Includes an `/healthz` endpoint, seat-hold + bot cover on disconnect (90 s reclaim window), and fake-lag knobs (`MC_FAKE_LAG_MS` / `MC_FAKE_JITTER_MS` / `MC_FAKE_LOSS`) for netcode testing. Boots via an esbuild launcher (`packages/server/run.mjs`); room behavior covered by a real socket round-trip test suite.
- **Online play through one net interface:** the client's game runtime now speaks to a `NetLink` adapter — `WorkerLink` (offline Web Worker sim) and `SocketLink` (Colyseus) are interchangeable, so offline and online share every system above the transport. `?online=1` routes Bridge Brawl through the server (`?server=` overrides the endpoint; production defaults to same-origin `wss://…/ws`). The HUD shows a color-coded RTT chip online, and a transport drop lands on a clean failure veil ("Match lost to the void" + reason + back to hub) — never a stuck client.
- **Movement prediction + reconciliation (TECH §6):** the local champion starts walking the instant you click — a client-side navgrid walk at authoritative move speed — while the server confirms one trip later. Reconciliation compares the server state against the *recent predicted trajectory* (a snapshot is one trip old; riding behind on the walked path is on-track, not an error), drains genuine divergence rate-capped (~10 u/s, ≤0.8 u per snapshot, every step glide-smoothed over 100 ms) so corrections read as a catch-up slide instead of a rubber-band, and hard-snaps only when the server itself jumped (dash/hook/respawn). Remote entities interpolate 100 ms behind arrival through a snapshot ring with a 100 ms extrapolation cap. The rules live in a pure, unit-tested module (`predict.ts`, 8 tests).
- **Lag-simulation acceptance smoke (`scripts/smoke-lag.mjs`, in CI):** boots the real server with 150 ms delay + 30 ms jitter + 2% loss and plays an online match in headless Chromium, asserting the click-to-move response beats the round trip (~0.35 u moved within 250 ms at ~650 ms measured RTT), no correction step ever reads as a jump (≤1 u), and divergence stays bounded — the ROADMAP "playable at 150 ms/2% loss" checkbox, executable.
- **Death-screen damage recap (UI_UX §10):** the death screen now opens with **"What killed you"** — the top 3 damage sources of your last 12 seconds, each with the killer's champion chip, the exact ability (Q/W/R name with its icon), attacks, passives, items, or structure, and a relative damage bar. Attribution runs through the sim's single damage funnel: every ability cast tags its slot, spawned projectiles and kegs carry their casting slot to impact, item procs and passives label themselves, and structure/mini damage classifies by source — so the recap teaches real counterplay, not guesses. Frozen at the moment of death, cleared on respawn, delivered through the codec's rare block (one resend per death). Covered by sim tests: per-ability aggregation, top-3 ordering, the 12 s window expiring old chip damage, and respawn clearing.
- **Quick-chat (GAME_DESIGN §17):** hold **C** for a 4-phrase radial — Nice! / Thanks! / Help! / On it! — mirroring the ping wheel. Lines are team-scoped, validated against a server-side whitelist, rate-limited (700 ms), and never AFK-flag you; they land as gold-edged toast lines under the ally frames with a soft cue. Offline the wheel echoes locally. Server test proves the enemy team never hears you and free-text injection dies at the whitelist.
- **Deploy stack, observability & soak (TECH §1/§11/§14):** `DOMAIN=play.example.com docker compose up -d --build` is now the whole production story — a multi-stage `Dockerfile` produces a **game** image (the Colyseus server as one self-contained esbuild bundle, verified to boot standalone) and a **web** image (Caddy with automatic HTTPS serving the built client and reverse-proxying everything under `/ws` — websockets, matchmake calls, lobby lookups — to the game), plus an uptime-kuma **status** page; CI builds the compose images per commit. The server got real observability: pino JSON logs for every room lifecycle event and Prometheus metrics at `/metrics` (live rooms/clients, sim-tick histogram, snapshot bytes, join/leave churn). The **200-socket soak** runs in CI as its own job: 25 rooms × 8 real WebSocket clients issuing orders for 45 s — every client's sim keeps advancing, tick p95 holds the ≤ 4 ms budget, downstream stays ~3 KB/s per client, and memory stays flat within bounds. README documents the VPS deploy, staging, and the dev server path.
- **Binary delta-snapshot codec (TECH §6):** online snapshots now travel as binary frames instead of JSON — positions quantized to 0.01 u, timers to 0.1 s, and every entity split into dirty-masked blocks (position / vitals / progress+stats / cooldowns / cast-state / buffs / passive readouts / rare-JSON) so only what changed crosses the wire and idle entities cost nothing. Baselines (self-contained keyframes with the full string table) go out every ~2 s and immediately when a client joins or rejoins mid-match, so late arrivals and refreshers key cleanly into the stream. Damage and fx events — the teamfight firehose — encode binary too; low-rate events stay JSON. Ticking passive readouts (Rook's Stonewall timer, Sylva's pollen tracker) get their own compact block precisely because JSON would have resent them every frame. The per-client intent ack is patched into a fixed header offset on a per-client copy of the shared team buffer. Measured on a real elite-bot mid-game brawl: **~10.4 KB/s per client average (409 B typical delta, 5.1 KB keyframes)** against the ≤ 12 KB/s budget — enforced forever by `codec-budget.test.ts`, with round-trip fidelity, brush re-entry, late-join, and periodic-resync covered in the protocol test suite. All online smokes (lag, lobby, reconnect) now run over this wire.
- **Reconnect, AFK takeover & clean failure (GAME_DESIGN §17):** every seat message carries a one-time **rejoin ticket** persisted to sessionStorage — closing or refreshing the tab mid-match boots straight back into the *same* match (clock continuous, same seat, ~6 s on the CI machine; a used ticket mints a fresh one, a dead room burns it so boot never loops). While you're gone the server's cover bot plays your seat and hands it back the moment you return. Going **AFK** (45 s without input) does the same thing live: a bot takes over, an amber banner says so, and any input reclaims the seat instantly. Clean failure got teeth too: `scripts/smoke-reconnect.mjs` in CI kills the server mid-match and asserts the "Match lost to the void" veil inside 10 s with a working route home. Dropping during the loading screen now also bot-covers your seat instead of stranding it.
- **Custom lobbies with codes (UI_UX §5):** "Play with friends" in the hub creates a lobby or joins one by 6-char code (`?join=ABC123` copy-links deep-link straight into the join sheet). The lobby screen shows two team columns of 4 seats — tap an empty seat to move there; the leader drops bots on seats and tunes each one's tier (Recruit/Veteran/Elite) right on the chip — plus a huge invite code with copy-code/copy-link, a bot-fill toggle, non-leader readiness, and a leader START gated on everyone being ready. Edge states are real: crown migration with a toast when the leader leaves, a friendly "make your own lobby" error on dead/full codes, mid-select deserters become bots that keep their pick. START runs the champion-select deal **server-side** (per-human deal + 2 rerolls, shared team bench with atomic swaps, 45 s auto-lock, bot picks dealt with the same no-duplicate rule) and hands every human a one-time seat reservation into a private BridgeRoom — walk-ins without a token are rejected, wire identity stays server-assigned. The lobby survives the match: the hub shows an ambient party dock while you browse, and Play Again returns the whole party to the same lobby for the next round. The select ceremony itself is now presentation-only (`SelectCeremony` + a driver), so offline and lobby matches share the exact same deal UI, and the whole HUD renders ally/enemy relative to *your* seat (team frames, killfeed, minimap — mirrored for team 1 so your base is always left — scoreboard, victory slab, podium, fountain/gate tinting): sitting on the red side reads identically to the blue side. Room loading now waits for every reserved human (20 s cap per UI_UX §7; a bot stands in until the slow loader arrives). Covered by a full server round-trip test suite and a CI smoke that drives two real browsers — create, deep-link join, bots, ready/start, server deal, both land in the same online match.

- **Nightly balance sweep:** a scheduled workflow (`.github/workflows/nightly-balance.yml`) runs 210 mixed-tier bot matches across three seed shards every night (`workflow_dispatch` for on-demand runs with a custom size), each shard emitting a JSON summary (`botmatch.mjs --json`) that `balance-aggregate.mjs` folds into a combined winrate table on the run summary — the big-n read the balance annotations point at, where ±7–8% small-sample noise stops dominating. Red only when matches fail to finish; band status is a report. The game server also logs a **sim state hash every 30 s** per room, arming the ROADMAP "no desync" spot checks for the first real multi-human session.

### Changed
- **Bot micro (veteran and elite):** bots now **focus-fire the softest effective target** in reach (HP × armor weighting — a healthy paper-armor gunner outranks a half-dead vanguard), **commit to closing on longer-ranged harassers** instead of eating free poke on the march, but only with local numbers (never diving a supported kiter — the ungated version measurably fed), and **aim heal-bearing casts at the sorest ally in range** instead of the enemy's feet (data-driven: any ability whose actions heal qualifies — Sylva's Blooming Ward today). Recruits stay naive by design.

### Balance
- **v0.3 compensation round for the smarter bots** (the focus-fire meta punishes squishy frames): Fathom range 6.0 → 5.7 and AD 58 → 56 (third trim — still runs hot); Rook HP 1080 → 1050; Grukk AD 67 → 65; Rattle HP 740 → 765 and armor 28 → 31; Mortis armor 20 → 24; Sylva HP 680 → 705, armor 21 → 24, Blooming Ward heal 40 → 46/s. Across 56 mixed-tier matches on two seeds: Grukk 58% / Rattle 42% in the 40–60 band, Sylva 39% at the edge, Mortis ≈33% cold, Fathom/Rook ≈64% hot — honest residual annotated in ROADMAP; the deep re-tune runs against the big-n nightly sweep where per-round noise (±7–8% at n≈40) stops dominating. All numbers mirrored into docs/CHAMPIONS.md.

## [Unreleased] — v0.2.0 "Shatterbridge" (in progress)

### Added
- **Offline play (airplane mode):** a service worker precaches the entire build at install — app shell, hashed bundles, game assets, icons, fonts (the list is injected by a Vite build step, so caching never depends on which first-visit requests raced past worker activation). After the first visit the full game — hub, champion select, Bridge Brawl vs bots — boots and plays with no network. Verified in CI by the bridge smoke's offline-reload leg.
- **Acceptance harness upgrades:** `scripts/botmatch.mjs` now reports per-tick p95 (~0.4 ms vs the 4 ms budget) and `scripts/smoke-bridge.mjs` runs the full acceptance path in CI: select (reroll + bench swap) → rigged win (`?rig=win`) → slab → podium → summary → history → offline reload.
- **Bridge Brawl simulation (full match loop, sim-side):** The Shatterbridge map def (deck, rails, brush pockets, orb pads, gates, lane), Watchtowers (Mini-first aggro, instant switch onto champion-attackers, +40% ramp, backdoor protection, destroy-in-order invulnerability, nav stamps that open when a tower falls), Clash Core (defense pulse, exposure after both towers, destruction → victory freeze + `matchOver`), Mini waves every 25 s (Bruiser/Zapper/Ram with per-kind AI: lane marching over A*, aggro/leash, zapper missiles, ram siege priority, +3%/min scaling), spawn-barrier walls that drop at 0:20, health orbs on the pads (heal + energy + XP with ally splash), brush concealment (hidden enemies are omitted from snapshots — map-hack impossible; attack/cast reveals for 1.5 s).
- **Economy:** starting 500 g, ambient XP/gold (half while dead), Mini bounties (gold split / XP full within 10 u), champion kill credit (killing blow or latest damager, 10 s assist window, streak bounties capped +300, assist pool split, takedown XP split), tower bounties (150 g global + 90 XP), levels 1–10 from the XP curve with +4% heal level-up moments, R hard-gated until level 4, death timers `5 + 1.5×level` (cap 20 s), fountain-plate fast heal.
- **Items live in the sim:** stat aggregation into champion totals (incl. haste-scaled cooldowns and the 2.5 attacks/s cap), shield absorb pools, and all 12 passive hooks (windrunner, executioner, stormweaver, juggernaut, nullwave, lifesteal, dragonfang, starcore burn, titan, phantom, lifebloom, overclock stub) + the 4 relic actives (Blink Prism, Purge Bell, Ember Flask, Horn of Rally). Shop honors the fountain/death windows and component upgrades (T2/T3 consume + discount their base item).
- **Bot AI (in-sim, deterministic, honest):** Recruit/Veteran/Elite tiers with per-tier reaction cadence (0.4 s → 0.2 s), skillshot aim noise + velocity lead, retreat thresholds with hysteresis, orb rotations, wave-front push discipline, structure-siege commits, elite skillshot dodging/kiting/focus-fire/ult-holding, relic usage, per-champion build orders, and Aggro/Guardian/Objective/Chaotic personalities. Bots read the same brush-concealed state as players and issue ordinary intents.
- **Balance harness:** `scripts/botmatch.mjs` runs seeded headless bot matches in bulk and reports lengths, winners, team winrates and champion K/D/A.
- **Champions +4 (sim + data + Training Grounds visuals):** **Mortis** (Soul Ledger inscriptions with attack refunds, Soulbolt that carries through Mini kills, telegraphed Grasping Stacks root — longer vs inscribed, Overdue Maelstrom moving channel), **Rattle** (Loose Bones dash charges, Fan of Knives, Rattle Step dash + destructible skull return-blink, Marrow Harvest blink-strike with kill-refund watch), **Grukk** (Toll Paid damage stacks, Skewer tip-pull lunge, War Bellow that fears Minis + self-shield, three-slam Seismic Tantrum via multi-charge recast), **Sylva** (Pollen Trail walking garden, blooming Thorn Dart, Blooming Ward heal/cleanse/dampen zone, Wildwood Embrace roots extended per bloomed flower). New generic sim capabilities backing them: delayed/telegraphed areas, caster-following channels, dash sweeps with tip-pull, destructible markers + return blinks, blink-strikes, self-shields, fear (Minis scatter), damage-amp buffs, flower/zone entities, projectile fans and kill-piercing, multi-charge recasts — all data-driven opcodes with zod schemas. Per-kit Vitest coverage in the Training Grounds; FX timelines + synth cues authored for all four kits and the bridge structures/relics.
- **Protocol v0.2:** shop/relic intents, Mini/Tower/Core/Orb snapshots, match-state snapshot (clock, barrier, team kills, towers, next orb, victory), kill/towerDown/matchOver/purchase/barrierDown events; snapshots now carry gold/K/D/A/items/relic/shield/brush state per champion.
- **Bridge Brawl is playable from the hub (client):** the mode card launches a full 4v4 vs Veteran bots (1 human + 7, champion pool dealt without duplicates). The Shatterbridge renders as a real sky-bridge — deck-clipped fractured floor with frayed rail edges, continuous low rails, underdeck pillars, drifting rock islets and trees in the void, brush pockets (bushes + grass), orb pads, team-flagged gate lines, fountain plates with glow discs and banners, and battle wreckage. New battle actors: Watchtowers (Castle hexagon stack + ballista that aims at its target, team-flat roof, ramp glow, aggro tether — bright red when it targets *you*, invulnerability dome, roof blown off under 66% HP, lean + chimney smoke under 33%, full collapse into persistent rubble), Clash Core (plinth + floating team crystal, halo, shield dome until exposure, implosion on destruction), Minis (Blocky bodies with team tints, walk/attack/die clips, spear-armed Bruisers, shooting Zappers, Rams as rolling Castle siege engines, per-kind death FX incl. a collapsing ram wreck), health orbs (bobbing glow), Sylva's flowers and ward zone, Rattle's hovering skull marker (kegs are now data-driven bodies). Spawn barrier walls render as animated team-tinted energy fields that dissolve at 0:20; Overtime/Sudden Death shift the fog toward ember. Brush-hidden allies fade translucent (enemies are omitted server-side). Champion/mini/structure health bars are team-colored; right-click targeting picks enemy units only.
- **Champion Select v1 — "the Deal":** entering Bridge Brawl now runs the select ceremony — staggered card-flip deal of your champion and the three ally picks (bot names on the cards), two rerolls with a burst animation, rerolled champions slide to a team bench and swap back with a tap, hidden enemy silhouettes, a resolved-numbers passive tip for the held champion, and a 45 s timer ring around LOCK IN that auto-locks on timeout. The locked 8-seat lineup is exactly the roster the match simulates, and the loading veil shows your team's cards while the bridge loads.
- **Bridge HUD complete (UI_UX §8/§10–§12 for the v0.2 feature set):** match strip (team kills, tower pips, clock, gate countdown, orb timer) + OVERTIME/SUDDEN DEATH banner; team frames for both sides (portrait chips, levels, HP bars, respawn counts, brush-hidden enemies dim to "?"); killfeed (champion-chip kill cards, tower falls, surrender); lane-strip minimap (structures, champions, minis, orbs, self ring); gold + 6 item slots + relic slot with cooldown; **death screen = the shop** (T1/T2/T3 columns + relic row with per-item icons, component discounts shown, owned/affordable states, one-click buy with hammer-clink, sell row, denial toasts); hold-**Tab scoreboard** (both teams: level, K/D/A, items, gold, hidden markers, structure tallies); **pings** — hold-G radial wheel (Danger / Attack / Help / On-my-way) + Alt+right-click quick ping, world pulse rings and per-kind cues, **and sim-side bot reactions** (Danger forces nearby allies to disengage; rally pings outrank the core march for ~4.5 s); FTUE coach-marks (move → abilities → ping → shop-on-death, shown once); **surrender** in the Esc menu from 8:00 (solo-vs-bots concedes immediately, vote UI arrives with multiplayer); and the **end-of-match sequence** — VICTORY/DEFEAT slab → podium (MVP + runners-up with K/D/A) → match summary with **Play Again** re-queue and local match history (last 30, in localStorage). Ability icons for all six champions plus 22 distinct item/relic icons (game-icons.net, credited). The death-screen damage recap ships with damage attribution in v0.3 (noted in UI_UX §10).
- **Fixes that fell out of making the bridge playable:** clicks past the deck rails now clamp to the nearest walkable cell (the void is nav-stamped; orders used to dead-end silently), champion/keg materials are cloned per instance (hit-flashes and fades no longer leak across duplicate champions), prop sockets prefer dedicated weapon slots over bare hand bones, glow sprites use a soft radial texture instead of rendering as squares, and the Enter keypress that submits the guest-name screen no longer instantly launches a match from the hub.

### Fixed
- **Champions no longer respawn lying down.** Finished one-shot animation clips (`clampWhenFinished`) held their final pose at full weight forever, so the death clip's lying pose kept blending into everything after respawn — and interrupted casts leaked the same way. Finished and interrupted one-shots now fade out properly.
- **Right-click on an enemy now attacks it** (standard MOBA scheme — no more walk-then-left-click); empty ground still moves. Mirrored in GAME_DESIGN §15.
- **Removed the click-to-move sound** — the move marker ring stays, the ping noise is gone.
- **Post-lane-phase stalemates:** rigged-structure smoke runs exposed that once towers fall, bot armies trade at mid indefinitely until Overtime (matches still end inside the sudden-death bound — the 12–18 min target gets a dedicated balance pass). Smoke tests now use `?rig=win` (paper enemy structures + idle enemy seats) to exercise the win sequence deterministically.
- **KayKit champions (Mortis, Rattle) rendered as collapsed blobs.** Two root causes: the asset pipeline measured bounding boxes *after* `quantize()` (which rewrites positions into normalized grid space), so the manifest bbox inflated and the client's normalization scale collapsed the model; and prop-socket lookup missed GLTFLoader-sanitized node names (`handslot.r` → `handslotr`), so weapons fell back to the model root. Bounds are now measured pre-transform, socket lookup checks candidates in priority order (dedicated weapon slots before bare hand bones) across both rig families, and the two KayKit rigs carry visual-scale compensation (1.35× / 1.3×) for the T-pose bbox headroom of their hat/hood.
- **Deployments 404'd on `game-assets/manifest.json`** (e.g. Vercel with the Vite preset): hosts that ran the client's bare `vite build` shipped without the pipeline-generated assets. Added a repo-root `vercel.json` (install/build/output wired to the root `pnpm build`), a Vite build guard that fails with an actionable message when `public/game-assets/` is missing, and a README **Build & deploy** section.
- **Vercel builds now succeed under any project configuration:** the client's `build` script runs the asset pipeline itself (`pnpm -w assets:build && vite build`) and a second `vercel.json` inside `packages/client` covers projects whose Root Directory points there — no dashboard setting can skip the pipeline anymore. The pipeline also launches through an esbuild bundler (`run.mjs`) instead of Node's version-dependent TypeScript stripping (`--experimental-strip-types` is 22.6+-only and had hard-failed the build), with the repo root located by workspace-marker walk-up so the bundle runs from anywhere.

### Balance
- **v0.2 balance pass (7 harness-driven rounds, ~250 seeded matches):** Match pacing — Mini scaling 3 → 4.5%/min, Core pulse 120 → 95, tower backdoor protection 50% → 35%, Overtime moved 16:00 → 15:00 with Corebreaker upgraded (core damage ×3, 5 Rams per wave), Sudden Death moved 20:00 → 17:30 with 1.5%/s decay. Result: every harness match finishes, lengths 8–18.6 min (avg ≈ 18 mixed, 17.3 Elite). Champions — **Fathom** reined in (HP 640→615, AD 62→58, AS 0.88→0.80, MS 3.6→3.45, range 6.5→6.0, Skipshot 90→70 base, Powder Rounds 30→20); **Rattle** toughened (HP 660→740, armor 24→28); **Grukk** sharpened (AD 63→67, War Bellow shield 164→190 base); **Mortis** empowered (HP 600→650, Grasping Stacks 95→112 + root 1.2→1.4 s, Maelstrom tick 70→82); **Sylva** bolstered (HP 620→680, Ward heal 30→40/s, Wildwood Embrace 120→150). All numbers mirrored into docs/CHAMPIONS.md and GAME_DESIGN. Residual bot-pool spread (Fathom hot, Sylva cold vs mixed tiers) tracks bot micro and is scheduled with the v0.3 bot-AI pass — noted in ROADMAP.
- Watchtower HP 3000 → 2400 and Clash Core 4200 → 3600; backdoor-protection radius 8 u → 11 u; Rams every 3rd wave → every 2nd; Bruisers/Zappers deal 2.5× to enemy Minis. First harness runs showed doc-spec sieges stalling matches past 25 min (waves stockpiled, structures outlived every push window); numbers mirrored into GAME_DESIGN §13.
- Champion takedown XP (60 × victim level) is split among killer + assisters (was effectively full-to-each in early implementation — blob kills snowballed levels).
- New: 500 g starting gold and fountain-plate regen (9% max HP + 15 Energy/s) — documented in GAME_DESIGN §12.

## [Unreleased] — v0.1.0 "Training Grounds" (in progress)

### Changed
- **UI direction locked (binding for all future screens):** hero-shooter menu language — sharp corners, skewX(−10°) parallelogram tabs/ribbons, italic-skewed Oswald display type + Barlow Semi Condensed UI text, flat panels with hairline borders and gold top edges, dark chrome nav bar, light diamond-pattern and dark streaked backdrops, gold selection accent, keycap hints, CSS-mask iconography (no emoji). Hub rebuilt as nav-bar + hero cards + mode tiles; HUD, settings, boot/name, Esc menu and death veil restyled to match. Spec lives in ART_DIRECTION §8.

### Added
- **Monorepo & tooling:** pnpm workspaces (data/sim/protocol/client/tools), TypeScript strict, Biome, Vitest, package-boundary checker, GitHub Actions CI (check · test · build + headless visual smoke with screenshot artifacts).
- **`@mini-clash/data`:** typed content definitions with zod validation — Rook & Fathom kits, dummy units, Training Grounds map, FX timelines, procedural SFX recipes, UI strings.
- **`@mini-clash/sim`:** deterministic 30 Hz simulation — PCG32 seeded RNG, navgrid A* with string-pulling and dynamic wall stamping, declarative ability-action interpreter, auto-attacks with homing missiles, champion passives, powder kegs, volleys, leaps/knock-ups, target dummies with DPS windows, trainer commands, state hashing for replay tests.
- **`@mini-clash/protocol`:** intent/snapshot/event contract shared by the worker link (and the v0.3 server later).
- **Asset pipeline:** manifest-driven gltf-transform optimization of the CC0 packs (23 shipped assets, 1.4 MiB total incl. skybox), per-asset metadata (bounds, clips, skeletons), budget enforcement.
- **Client:** boot → guest name → hub flow, settings (video/audio/controls/accessibility with live remapping), Three.js toon-ramp renderer with follow camera, shake and hit-stop, worker-hosted offline sim, snapshot interpolation, animated champion actors with prop sockets, instanced particle system, data-driven FX runner, WebAudio synth engine, in-world health bars/damage numbers/aim telegraphs, Training Grounds HUD (ability cooldown wipes, trainer panel, dummy DPS readouts, champion switcher, death/respawn flow, Esc menu).

### Fixed (during headless visual verification)
- zustand object-selector infinite re-render on the HUD; instanced floor vanishing to frustum culling; clicks using a frame-stale cursor; UnrealBloom NaN blackout (bloom removed, glow via additive sprites); black characters from missing normals on unlit exports; software-GL context loss (SwiftShader now clamps to the low quality profile); arena wall scale; shadow acne.

## [0.0.2] — 2026-07-23

### Docs
- Complete planning suite authored: README, ROADMAP (v0.1 → v1.0 phase plan with acceptance criteria), TECHNICAL_ARCHITECTURE, GAME_DESIGN (Bridge Brawl ARAM ruleset, Shatterbridge map spec, combat math, item shop, Tag Team / Power Surge / Living Bridge systems), CHAMPIONS (12-champion launch roster with full kits, stats and animation specs), AUGMENTS (system + 48-augment generic catalog), UI_UX (all screens & HUD), ART_DIRECTION (visual/audio style guide), ASSET_CATALOG (inventory, licenses, sourcing plan), CLAUDE/AGENTS working agreements.
- Core decisions locked: 4v4 team size · Tag-Team duos + Power Surge drafting + Living Bridge events as signature systems · TypeScript + Three.js + authoritative Node stack · earn-only F2P economy for v1.

## [0.0.1] — 2026-07-23

### Assets
- Imported the base CC0 3D asset library (23 packs): Kenney (Mini Arena, Minidungeon, Mini Forest, Nature, Castle, Graveyard, Pirate, Space, Factory, Tower Defense, Survival, Holiday, Watercraft, Platform, Cute Characters, Blocky Characters, Cube Pets, Skyboxes) and KayKit (Dungeon, Medieval Hexagon, Skeletons + animation library, Spooktober, Fantasy Weapon Bits, RPG Tool Bits).
